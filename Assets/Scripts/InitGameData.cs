using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InitGameData : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        bool tmp = GameData.isPlayerDead;
    }

}
